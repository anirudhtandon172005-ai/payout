{
  "name": "payout",
  "version": "1.0.0",
  "description": "PayOut — SPA",
  "main": "index.html",
  "scripts": {
    "dev": "npx http-server -p 8080",
    "start": "npx http-server -p 8080"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
